import StripeCheckout from "react-stripe-checkout";

const Checkout=()=>{

    const onToken=(token)=>{
        console.log(token);
    }
    return(
        <>
        <div className="container">
            
        </div>
        </>
    )
}

export default Checkout;